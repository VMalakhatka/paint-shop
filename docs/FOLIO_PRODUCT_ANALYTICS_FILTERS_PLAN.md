# Фильтры товарной аналитики ФОЛІО

Проверено: 2026-08-31 по WordPress consumer, контракту Java schema v2,
коду выгрузки карточек и подтверждённым полям ФОЛІО.

Статус документа: рабочий контракт реализованной части и план следующих этапов.
Разделы с явной пометкой «План» не являются доступным поведением интерфейса.

Полная матрица возможных отборов, границы подтверждённых источников и задание на
расширение Java находятся в
[FOLIO_PRODUCT_ANALYTICS_SCENARIOS_BACKEND_TASK.md](api/FOLIO_PRODUCT_ANALYTICS_SCENARIOS_BACKEND_TASK.md).

## Текущее состояние

Раздел ниже описывает исторический этап schema v2. Текущее состояние v4/v5:
[карта реализации](BACKEND_GUIDE.md#товарная-аналитика-карта-реализации).
Исходный процесс владельца для следующего этапа закупки зафиксирован в конце
этого документа; он не означает, что поправка спроса уже реализована.

Страница `Price Sync -> Product analytics` читает активное завершённое поколение
таблиц `folio_product_*` в MariaDB и ничего не изменяет в ФОЛІО. Все запросы одного
отчёта привязаны к одному `generation_id`; незавершённые и старые поколения не
смешиваются. Для schema v1 интерфейс требует повторно сформировать снимок и не
показывает отсутствующие показатели как нули.

Реестр товаров строится для одного склада и поддерживает:

- поиск, экономическое состояние, состояние проверки, alert/status/severity;
- множественный include/exclude текущих поставщиков и отдельный контроль
  `CURRENT`, `MISSING`, служебного значения `1`;
- диапазоны физического, зарезервированного и доступного остатка, учётной цены и
  вложенного капитала;
- общие, регулярные и разовые продажи за 30/90/365/730 дней;
- выручку, валовую прибыль до возвратов, маржу, средний капитал, оборачиваемость,
  GMROI и покрытие;
- даты продаж, регулярных продаж, приходов, движений и предупреждений.

Состояние `LOW_MARGIN` подписано по фактическому правилу backend как отрицательный
валовый результат за 3 месяца; оно не обещает настраиваемый порог низкой маржи.

Отдельная вкладка движений читает `folio_product_movement_fact` того же активного
поколения. В ней server-side работают период, SKU, номер документа, исходный вид
операции ФОЛІО, тип документа, класс и направление движения, режим спроса, условия оплаты,
сегмент клиента, признаки влияния, контрагент и текущий поставщик. Этот реестр
показывает факты и не пересчитывает агрегированные карточки товаров.

На отдельной странице `Price Sync -> Analytics scenarios` пользователь создаёт
версионируемые сценарии аналитики. Один сценарий хранит выбранный склад, условия
реестра товаров, условия реестра движений и вкладку, открываемую по умолчанию.
Сценарий может быть общим для менеджеров или личным, активным или архивным.

Страница `Product analytics` применяет выбранный сценарий сразу к вкладкам
`Products` и `Product movements`. Локальные изменения полей считаются временными и
не перезаписывают сохранённый сценарий. Верхние карточки итогов пока показывают
весь выбранный склад до фильтров сценария; это явно подписано в интерфейсе.

Старые именованные наборы фильтров товара из user meta один раз импортируются как
личные сценарии. Исходный user meta не удаляется и остаётся доступным для отката.

Подписи страницы локализованы на украинский и русский. Источник истины для расчётов
и классификаций — Java projection активного снимка, а не повторная бизнес-логика в
WordPress.

## Уже доступные измерения

В текущей схеме снимка или фактов движений подтверждены:

- склад, артикул и название товара;
- текущий поставщик и состояние определения поставщика;
- обычные и разовые продажи, выручка и валовая прибыль;
- класс движения: продажа, возврат, приход, перемещение, сборка, брак, внутреннее
  использование, маркетинг, инвентаризационная корректировка, резервирование и
  другие подтверждённые классы;
- тип документа и вид операции ФОЛІО;
- направление движения остатка;
- влияние движения на остаток, финансовые продажи и плановый спрос;
- режим спроса, включая разовый заказ;
- условия оплаты;
- сегмент клиента и контрагент;
- количество, стоимость запаса, выручка, прибыль, оборачиваемость, покрытие и даты
  движения/продажи.

`ПЕРЕМЕЩЕНИЕ` должно отображаться как направление `TRANSFER_IN` или
`TRANSFER_OUT`. Операции с подтверждённым признаком `РАЗОВ...` относятся к режиму
спроса `ONE_OFF_ORDER`. Это разные измерения, их нельзя объединять в один фильтр.

Для склада 7 backend подтвердил контрольный результат schema v2: 36 534 товара,
36 526 назначений поставщика, 8 отсутствующих назначений и 161 различное значение.
`current_supplier='1'` показывается как служебный код, требующий проверки. Ошибка
AJAX не заменяется текстом «поставщиков нет»: интерфейс показывает отдельную
ошибку загрузки с сообщением сервера.

Фильтр «без коммерческих продаж» не требует наличия остатка. Представление
«капитал без продаж» является отдельным условием: положительный вложенный капитал
и отсутствие регулярного спроса за выбранный период.

## Чего в текущем снимке нет

Пока не подтверждены как поля аналитического снимка:

- товарная группа, подгруппа, бренд и отдел;
- lead time поставщика, MOQ, кратность упаковки и товар в пути;
- полноценные net-показатели с возвратами;
- ежедневная история дефицита и длительность отсутствия товара.

Торговое оборудование, транспортные расходы и другие служебные SKU нельзя надёжно
исключать по тексту названия. Java должна передать стабильные идентификаторы и
названия товарной группы/подгруппы из ФОЛІО.

Часть будущих данных уже подтверждена в ФОЛІО или существующем Java-коде, но ещё
не входит в аналитический снимок: шесть уровней товарных групп, отдел, тип товара,
единица измерения, фасовка, минимальная партия и min/max stock. Бренд, штрихкоды,
склады обеих сторон перемещения, продавец, SCM lead time, открытые заказы
поставщика и товар в пути требуют отдельного исследования Paint_Ua. Эти категории
нельзя показывать одинаково как готовые фильтры.

## Граница текущего этапа сценариев

Сценарий версии 1 хранит только условия, которые реально поддерживает snapshot
schema v2. Новое поле становится активным в редакторе сценария только после того,
как Java:

1. подтвердит источник ФОЛІО и его семантику;
2. добавит поле в атомарный снимок/проекцию;
3. опубликует поле через capabilities;
4. применит фильтр server-side и вернёт его в `appliedFilters`;
5. добавит проверку итогов и совместимости поколений.

Сохранённый сценарий не должен создавать впечатление, что неподдерживаемый фильтр
работает. Неизвестное поле блокируется понятной ошибкой, а не превращается в
`ANY`.

## Единое правило фильтров

Каждое измерение получает один взаимоисключающий режим:

- `ANY` — не ограничивать;
- `INCLUDE` — оставить выбранные значения;
- `EXCLUDE` — исключить выбранные значения.

Значений может быть несколько. Пустой список означает отсутствие ограничения.
Интерфейс должен явно показывать режим и выбранные значения; скрытая логика
приоритетов запрещена.

Исключение товарной группы меняет всю анализируемую совокупность SKU и все итоги,
а не только скрывает строки таблицы. Исключённое оборудование не должно попадать в
остаток, вложенный капитал, оборачиваемость, мёртвый товар и кандидатов закупки.

Фильтры движений и документов должны пересчитывать показатели за период из
`folio_product_movement_fact` или подготовленной серверной агрегации. Их нельзя
накладывать на уже рассчитанные общие метрики только на стороне WordPress.

## План: многоскладской отчёт

Пользователь выбирает один или несколько складов. Отчёт обязан сохранять:

- общий результат по выбранным складам;
- раскрываемую детализацию каждого товара по складам;
- один и тот же источник данных, версию схемы, поколение и дату расчёта либо явно
  показываемое предупреждение о несовместимости.

Суммируются количества, стоимость запаса, выручка, себестоимость и валовая
прибыль. Производные показатели рассчитываются заново:

- оборачиваемость — из суммарной себестоимости и среднего капитала;
- GMROI — из суммарной прибыли и среднего капитала;
- покрытие — из суммарного доступного остатка и регулярного дневного спроса;
- состояние товара и предупреждения — по серверным правилам агрегированного
  отчёта.

Нельзя усреднять готовые проценты или выбирать состояние одного из складов.

### Покрытие запасом и доступность — разные показатели

Текущее `coverageDays` означает, на сколько дней должно хватить текущего
доступного остатка при среднем регулярном спросе выбранного периода. Оно не
показывает, сколько дней товар фактически отсутствовал.

Для фактической доступности нужен отдельный подневный слой и показатель
`availabilityPercent`. Он должен поддерживать одновременно:

- собственный процент каждого физического склада;
- настраиваемые группы складов с правилом «доступно хотя бы на одном применимом
  складе»;
- исключение склада из знаменателя конкретного SKU при `MIN_TVRZAP=0`;
- сохранение физической детализации, даже если объединённая группа показывает
  высокую доступность.

Проценты складов нельзя усреднять. Подробный backend-контракт и критерии приёмки:
`docs/api/FOLIO_PRODUCT_AVAILABILITY_BACKEND_TASK.md`.

## Порядок реализации

### Этап 1. Сценарии без изменения формул — реализован

- сценарий schema v4 хранит базу, один или несколько складов, период, условия
  товара и движения, расчёт, сортировку, пагинацию и вкладку по умолчанию;
- фильтры с режимами `ANY`, `INCLUDE`, `EXCLUDE` включаются только после успешного
  backend `capabilities` для выбранных складов;
- создана отдельная страница управления сценариями с общими и личными сценариями,
  архивом, копированием, историей версий и защитой от одновременного изменения;
- один сценарий применяется к обеим существующим вкладкам аналитики;
- временные изменения на странице отчёта не меняют сохранённый сценарий;
- старые личные наборы фильтров импортируются в сценарии без удаления исходных
  данных и без повторного создания дубликатов;
- товары, движения, totals и складская детализация получают единый server-side
  результат schema v4;
- воспроизводимая ссылка с параметрами отчёта пока остаётся отдельным улучшением.

Сценарии WordPress хранятся в таблицах с текущим префиксом сайта:

```text
{prefix}lps_analytics_scenarios
{prefix}lps_analytics_scenario_revisions
```

Профиль рабочего сценария имеет `schemaVersion=4`. Массив `warehouseIds` содержит
один или несколько складов, а многоскладские итоги пересчитывает Java. Старые
сценарии читаются через ограниченное преобразование: отсутствующие условия не
додумываются и не заменяются молча.

### План, этап 2. Классификация товара

- добавить в Java projection стабильные идентификаторы и названия группы,
  подгруппы, бренда и отдела;
- добавить include/exclude-фильтры;
- настроить сохранённый набор исключений для служебных групп без жёсткого списка в
  PHP.

Для рабочего интерфейса Java должна опубликовать как минимум:

```text
product_group_id, product_group_name,
product_subgroup_id, product_subgroup_name,
brand_id, brand_name,
department_id, department_name
```

WordPress не должен извлекать эти значения из названия товара.

### Этап 3. Несколько складов — реализован в schema v4

- серверный запрос принимает список складов и возвращает агрегированный результат;
- производные показатели пересчитываются по общим величинам, а не усредняются;
- интерфейс показывает общий итог и складскую детализацию;
- несовместимые снимки блокируют отчёт явной ошибкой.

Минимальный серверный контракт агрегированного запроса:

```json
{
  "sourceDatabase": "Paint_Ua",
  "warehouseIds": [1, 5, 7],
  "filters": {
    "productGroups": {"mode": "EXCLUDE", "values": ["SERVICE"]},
    "productSubgroups": {"mode": "ANY", "values": []},
    "suppliers": {"mode": "INCLUDE", "values": ["Kreul"]},
    "movementClasses": {"mode": "EXCLUDE", "values": ["TRANSFER_IN", "TRANSFER_OUT"]},
    "operationKinds": {"mode": "ANY", "values": []},
    "demandModes": {"mode": "EXCLUDE", "values": ["ONE_OFF_ORDER"]}
  }
}
```

Ответ должен содержать общий итог, строки SKU, детализацию по складам, доступные
значения справочников и точный аналитический контекст: поколения, версию схемы,
период и применённые фильтры.

### Этап 4. Движения и документы — реализован для capability-backed полей v4

- период и подтверждённые фильтры движения применяются server-side;
- фильтр типа документа остаётся отдельным от вида операции;
- фильтры движений пересчитывают показатели периода, но не текущий остаток;
- неизвестные или неподдержанные поля не отправляются как рабочие фильтры;
- полный экспорт активных отборов и воспроизводимая ссылка остаются улучшением.

### План, этап 5. Закупочная аналитика

- ABC по выручке и валовой прибыли;
- XYZ по регулярному недельному спросу;
- прогноз и точка заказа только после добавления lead time, MOQ, кратности и товара
  в пути;
- сначала предлагать перемещение избытка между выбранными складами, затем закупку;
- отдельно показывать дефицит, излишек, мёртвый товар и проблемы качества данных.

## Рекомендуемые фильтры интерфейса

| Блок | Значения | Доступность |
|---|---|---|
| Склады | один или несколько | реализовано в schema v4 |
| Поставщики | один или несколько текущих поставщиков | capability-backed v4 |
| Группа/подгруппа/отдел/тип товара | стабильные справочники ФОЛІО | capability-backed v4 |
| Бренд и дополнительные штрихкоды | стабильные справочники ФОЛІО | пока не рабочий фильтр |
| Движения | класс, направление, влияние на stock/финансы/спрос | capability-backed v4 |
| Документы | номер, тип документа и вид операции отдельно | capability-backed v4 |
| Спрос | обычный или разовый | capability-backed v4 |
| Клиенты | сегмент и контрагент | capability-backed v4 |
| Оплата | условия оплаты | capability-backed v4 |
| Показатели | количество, капитал, выручка, прибыль, покрытие, даты | реализовано для нескольких складов v4 |
| Качество данных | verification, supplier state, alerts | capability-backed v4 |

## Критерии приёмки будущего полного отчёта

- Для каждого отбора доступны режимы `INCLUDE` и `EXCLUDE`.
- Выбранные фильтры видны на экране и присутствуют в выгрузке.
- Итоги совпадают с суммой строк в том же аналитическом контексте.
- Разовые продажи входят в общие финансы, но не искажают регулярный спрос,
  coverage, XYZ и прогноз.
- Перемещения не считаются продажами и спросом.
- Исключённые группы не попадают в мёртвый товар и вложенный капитал.
- Многоскладские коэффициенты пересчитываются, а не усредняются.
- При неподдерживаемом фильтре отчёт блокируется понятным сообщением, а не строит
  приблизительный результат.


## Исходный Excel-процесс владельца и следующий preview

Зафиксировано: 2026-09-10. Источник — SQL владельца и три скриншота ФОЛІО
от 2026-09-10 (18:46:43, 18:47:35, 18:48:26). Это описание текущей ручной
работы и требования к развитию, не результат проверки живой базы.

### Подтверждено предоставленными материалами

- База отбора карточек — склад 7, `DOP2_ARTIC=Kreul`; исключены
  `NGROUP_TVR=Я_НЕИСП` и `Без названия`.
- Общий остаток «Опт + ХР + приём + ТР» — сумма `REZ_KOLCH` складов
  7, 10, 12, 9. Склады 1 (K1) и 5 (OD1) показаны отдельно.
  Склад 9 уже включён в сумму: прибавлять его повторно как транзит нельзя.
- Цена `ПАРТНЁР` со склада 7, основной штрихкод, группы, упаковка, MIN/MAX
  экспортируются вместе с карточкой. Цена партнёра не является доказательством
  себестоимости закупки. Поле `SCHET_ART AS трансп` — только alias в запросе,
  не подтверждённое количество товара в пути.
- Движение выбирается по Киеву, Киеву ОПТ и Одессе; период примера
  2026-05-01–2026-09-09. Сопоставление названий с ID 1/7/5 соответствует
  предоставленному контексту; ID не становятся константами новой модели.
- На фото отмечены «искл.»: `*ПЕРЕМЕЩЕНИЕ`, `*ПОЛУЧЕНИЕ`, `КОРРЕКТ.`,
  `*РАЗОВАЯ`, `ВОЗВРАТ`, `РЕКЛАМА`. Отмечены «вкл.»: `МУЛЬТИСБОРКА`,
  `РАСХОДНИКИ`, `*ПРЕДОПЛАТА`, `*РОЗНИЦА`. У розницы стоит «част.»,
  детали частичного отбора не раскрыты. У `СБОРКА` флажок снят: надпись
  «искл.» сама по себе не доказывает активный запрет.
- Остатки и движение соединяются в файл для расчёта заказа. По сообщению
  владельца, ручная модель не учитывает периоды отсутствия и товар в пути.
  Необходимо различать уже отражённый остаток склада 9 и ещё не поступившие
  ожидаемые поставки; точное содержание последних требует уточнения.

### Расхождения, которые надо устранить до сравнения рекомендаций

1. **Семантика остатка.** Source DAO Java передаёт `REZ_KOLCH` как
   `reservedQuantity`; `FolioProductEconomicsCalculator` вычисляет доступное
   как `KON_KOLCH − REZ_KOLCH`. Но Java business-документ `01_ACCOUNT.md`
   и skill reference `schema-map.md` описывают `REZ_KOLCH` как свободное
   количество, что согласуется с SQL владельца. Это существенное противоречие
   источников; пример KR-49600 ниже подтверждает расхождение по материалам
   владельца. Исправлять Java/source
   contract, тесты и поколения, а не компенсировать ошибку формулой PHP.
2. **Покрытие SKU.** Ограничения связанных таблиц в WHERE делают LEFT JOIN
   обязательными совпадениями: SKU без карточки хотя бы на одном из шести
   складов или без цены партнёра исчезает. NULL группы также не проходит `<>`.
   Новая модель должна показывать неполноту данных, а не терять такой SKU.
3. **Смысл расхода.** Java относит мультисборку к assembly, расходники к
   internal use; они не входят в `regularSoldUnits`. Для сравнения с Excel
   нужны отдельные количества «продажи», «потребление для сборки»,
   «внутренний расход». Включение последних в пополнение — отдельная политика
   сценария, а не переименование всех расходов в продажи.
4. **Контекст.** Склады спроса 1/7/5 отличаются от складов покрытия потребности.
   Хранение и приёмка могут снабжать продажи, не создавая собственный спрос;
   объединять их надо по настраиваемой группе. Транспорт остаётся отдельным
   источником, пока Java не подтвердит согласованность без двойного учёта.

### Контрольный товар KR-49600

Подтверждено скриншотами владельца 2026-09-10, 19:12:32 (Excel) и
19:16:04 (ФОЛІО); прямое чтение БД не выполнялось.

| Склад | Физический остаток ФОЛІО | Свободный остаток ФОЛІО | MIN | MAX |
|---|---:|---:|---:|---:|
| Киев | 19 | -11 | 20 | 20 |
| Одесса | 16 | -14 | 12 | 20 |
| Киев ОПТ | 607 | 142 | 1 | 9999 |
| Транспорт | 0 | 0 | 1 | 9999 |
| Приёмка | 0 | 0 | 1 | 9999 |

Excel показывает `K1=-11`, `OD1=-14`, объединённый остаток 142,
`EDN_V_UPAK=5`, `MIN_TVR…=1`, `need=7`, `moove=110`, `all=117`.
Склад хранения на втором скриншоте не виден; его отдельное значение не установлено.
Совпадение K1/OD1 со свободным остатком и исходный SQL подтверждают назначение
`REZ_KOLCH` в данном процессе. При этих входах существующая Java-формула
доступного даёт 30, 30, 465 вместо -11, -14, 142 соответственно.
Эти разности являются вычислениями; состав резервирующих документов не проверен.

Сумма показанных свободных остатков `142 - 11 - 14 = 117` совпадает с `all`.
Последующее чтение файла подтвердило `need=all-moove`: 7 — положительная
разница, а количество заказа вводится отдельно в C (см. ниже). MIN=1 в
экспортной строке относится к оптовой карточке, а не заменяет MIN 20/12
физических розничных складов.

Инварианты будущей проверки: отрицательный свободный остаток сохраняется как
дефицит доступного запаса; он не равен физическому отсутствию (на Киеве есть 19,
на Одессе 16). Физическая история v5 и доступность для нового заказа — разные
показатели. Резервы нельзя вычитать второй раз из уже свободного остатка.
Внутренние резервы/перемещения между членами группы требуют проверки связей
документов перед выводом о закупочной потребности всей сети.

### План улучшения относительно Excel

Сначала воспроизвести исходный отбор и объяснить расхождение по каждому
контрольному SKU. Затем сравнивать две рекомендации: по наблюдаемому спросу
и с поправкой на отсутствие. Обе показывают входные данные и причину отличия.

Кандидат поправки — регулярные продажи за подтверждённые дни наличия /
число этих дней; для группы нужен дневной контекст группы, рассчитанный Java.
Нельзя делить все продажи на доступные дни без согласования числителя:
продажа могла произойти в день с нулевым остатком к вечеру. Подневное наличие
v5 на конец дня является приближением, а не точной экспозицией продаж.
При нуле дней, короткой истории, неизвестном MIN, новой карточке или
несопоставимых каналах нужна отметка недостаточности данных; не бесконечный
спрос и не автоматическое увеличение заказа. Порог истории и ограничения
поправки должны быть параметрами проверяемой модели. Пока это не реализовано.

Java должна отдавать наблюдаемый и оценочный спрос, качество и покрытие
для согласованной группы. Preview использует срок поставки, целевой и страховой
запас, доступный остаток, подтверждённые поступления с датой/назначением,
перемещения, упаковку и MOQ. Физический транспорт и его поставщицкая часть
не складываются. Поступление после ожидаемого дефицита не скрывает сам дефицит.

Приёмка: контрольный набор с резервом, отсутствующей складской карточкой,
нулевыми продажами, длительным отсутствием, новой карточкой, внутренним
перемещением, мультисборкой и ожидаемой поставкой. Сохранить исходный отбор,
версию сценария, поколения, обе рекомендации и корректировку менеджера.
Лучшее качество подтвердить сравнением на отложенном историческом периоде,
а не только большим рекомендуемым заказом. Создание документов ФОЛІО,
резервов и реальных заказов в этот этап не входит.


### Проверка исходного файла kreul10092026.xls

Проверено: 2026-09-10, чтение BIFF-формул и сохранённых значений оригинала,
без изменения файла и без пересчёта в Excel. Два листа:
`Остатки_все_по_поставщикам` и `Лист1` (движение).

На основном листе 1 056 уникальных SKU, строки 2–1057. Владелец сообщил,
что обработал заказ до KR-17817 включительно — строка 356 (355 товаров).
После этой границы остаётся 701 товар; пустой заказ там означает незавершённую
работу, а не решение ничего не покупать.

- C без заголовка — введённое количество заказа: десять заполненных числовых
  ячеек, всего 66 единиц, все до строки 356 включительно.
- E (`need`) = G − F; отрицательное значение показывает недостаток относительно
  выбранного исторического расхода, но не является итоговым заказом.
- F (`moove`) — точный VLOOKUP по артикулу из `Лист1!A3:B1058`.
  B исходного отчёта подписана «Расх.колч.»; бизнес-смысл включает выбранные
  операции и не тождественен Java regular sales.
- G (`all`) = K + L + M: свободные остатки из отдельной выгрузки карточек.
- H = C × V (`CENA_VALT`); H1 суммирует H2:H1057. Код валюты требует своего
  справочника, его нельзя автоматически называть EUR или UAH.

По всем 1 056 строкам сохранённые значения E/G совпали с указанными формулами,
F — с исходным расходом по SKU; сохранённых Excel error cells не обнаружено.
Это проверка формул и кэша, не запуск пересчёта рабочего Excel.

Контроль KR-17817: C356=6, E356=-5, F356=11, G356=6, I356=6.
У KR-49600 строка 572: C пустая, E=7, F=110, G=117 — товар находится после
границы завершённой работы, решение по закупке ещё не зафиксировано.

В C149 (KR-17003), C157 (KR-17006), C237 (KR-17034) введено по 2 единицы
при I=6 (`EDN_V_UPAK`). Это наблюдаемое расхождение с жёстким ограничением
pack в текущем preview. Не считать размер упаковки автоматически минимальной
кратностью заказа и не исправлять введённые количества: подтвердить правило
поставщика, затем разделить справочную упаковку и обязательную кратность.
KR-17034 имеет need=0 и заказ=2, что дополнительно подтверждает ручное решение,
а не механическое преобразование need в заказ.

Начальные и конечные количества на листе движения относятся к отчёту с
отборами операций. Не использовать их как физическую историю наличия для
поправки stockout; текущие свободные остатки берутся с основного листа,
история физического наличия требует полного серверного источника.


### Решение по упаковке, 2026-09-11

Владелец подтвердил: для KR-17003/KR-17006/KR-17034 допустимы 2 штуки по одной
цене и упаковка 6 по более низкой. Текущий этап использует параметры базы и
предоставляет режим с упаковкой/без неё и ручной ввод. Импорт прайса реализован следующим этапом ниже; автоматический выбор ценовых
ступеней остаётся отдельной задачей. Режим реализован в WordPress preview и экспорте; отдельный
подтверждённый MOQ сохраняется. Исправление свободного остатка подготовлено в
Java schema 6. Актуальная приёмка и требования выпуска:
[регламент](OPERATIONS_RUNBOOK.md#обновление-preview-свободный-остаток-и-упаковка).


## Версии прайсов и варианты заказа поставщику

Источник: запрос владельца и read-only анализ `Kreul 2026.xlsx`, 2026-09-11.
Статус на 2026-09-11: импорт v1 реализован и проверен локально, production не менялся.
Загрузка XLSX, сопоставление GTIN, варианты VE, активация версий и дополнительные
отметки отсутствия доступны в коде. Версия и предложения закрепляются в preview
заказа и его экспорте. Выбор конкретного предложения, расчёт суммы заявки,
режим налогов и экспорт в форме поставщика остаются следующим этапом.
[Порядок импорта и проверки](OPERATIONS_RUNBOOK.md#импорт-прайса-поставщика).
Описанные далее требования к выбору предложения относятся к этому следующему этапу.

Основной лист `Preise 2025` внутри файла озаглавлен как прайс 2026, действует
с 2026-04-01. Название листа не задаёт год версии. Строки 132/133: артикул
поставщика 17003, GTIN 4000798170035, VE 2/6, базовая цена 6.69,
скидка 35%, F=4.3485; формат цены указывает EUR. В `Tabelle1` строки 127/128
дают базовые цены 6.69/6.38 соответственно. Нельзя смешивать источники или
выбирать меньшую цену из другого листа автоматически. На основном листе есть
Excel errors и особая отметка Netto; строка 8 имеет базовую цену и Netto,
но пустую F. Пустая F не является нулевой ценой, требует явного правила.

Реализованное хранение — отдельный каталог предложений поставщика в MariaDB
WordPress под владельцем `lavka-price-sync`; не варианты WooCommerce и не
перезапись складской карточки ФОЛІО. Java остаётся владельцем спроса, остатков
и аналитических метрик. Импорт/справочник поставщика связывает предложения с
его SKU и подтверждённым внутренним SKU; не дублирует Java-метрики.

Уточнение владельца 2026-09-11: вся информация файла поставщика сохраняется
как дополнительная для заказа и аналитики. Импорт не изменяет розничные,
оптовые, акционные или ролевые цены сайта, валютные/учётные цены ФОЛІО.
Оригинал и исходные поля, включая нераспознанные колонки, сохраняются вместе
с нормализованными предложениями. Сравнение цен возможно как аналитика,
но не запускает price sync или обновление карточек.

Сущности:

- версия прайса: поставщик, исходный файл в защищённом хранилище, checksum,
  лист/строка заголовка, дата действия, валюта, база цены (за штуку/упаковку),
  режим налогов только после подтверждения, схема сопоставления колонок,
  автор и время, статус draft/active/archived;
- исходная строка: оригинальный артикул поставщика и GTIN как строки, описание,
  VE, исходные цены, скидка, Netto, формула и cached value при наличии,
  номер строки и результат проверки;
- вариант предложения: ссылка на версию/строку, supplier article, GTIN,
  VE, подтверждённая единица/кратность заказа и MOQ отдельно, цена и валюта,
  способ получения цены, состояние сопоставления с внутренним SKU;
- выбор в preview: неизменяемая ссылка на вариант и версию прайса, артикул
  поставщика, количество штук, количество упаковок, выбранная цена и сумма,
  причина ручной корректировки. Новая версия прайса не меняет старый preview.

GTIN — основной ключ сопоставления с внутренним каталогом. Проверять допустимую
длину и контрольную цифру, сохранять исходный текст и ведущие нули; допустимые
форматы приводить к каноническому виду без потери идентичности. Числовые значения
Excel принимать только при доказуемом отсутствии потери точности. Один внутренний
SKU с несколькими строками прайса — нормальная связь один-ко-многим. Отсутствующий
GTIN, несколько внутренних SKU на один GTIN, конфликт цены для одинакового
варианта или несопоставленный товар требуют ручной проверки. Не создавать
товар автоматически и не угадывать соответствие из префикса KR-.

Рабочий поток: загрузить файл → выбрать поставщика/лист/колонки и подтвердить
валюту, единицу цены и правила Netto → получить preview сопоставлений, вариантов,
ошибок и изменений цен → подтвердить активацию версии. Повтор того же файла
с той же конфигурацией импорта не создаёт дубли. Старые версии сохраняются;
строки, исчезнувшие в новой версии, не удаляют историю прошлых заказов.

### Отсутствие товара в полном прайсе

Правило владельца: товар данного поставщика, отсутствующий в активированном
полном прайсе, отмечается **«Снят с производства»** в дополнительном закупочном
справочнике. Основание статуса — отсутствие в конкретной версии прайса по
принятому бизнес-правилу, а не отдельное подтверждение производителя.
Сохранять поставщика, дату, версию и причину; показывать статус менеджеру
при формировании заказа и в дополнительной аналитике.

Сравнивать с товарами выбранного поставщика, а не со всем каталогом сайта;
включать его существующие товары уже при первом полном импорте. При частичной
загрузке/добавлении строк отсутствие не меняет статус. До активации полного
прайса показывать список будущих отметок. Ошибка цены, например #N/A, не
означает отсутствие товара: строка остаётся присутствующей, цена — непроверенной.
Неразрешённое сопоставление GTIN не даёт права объявлять товар отсутствующим;
такие случаи остаются на проверке. Исчезновение одной упаковки при наличии
другой снимает вариант предложения, но не весь товар.

Отметка не удаляет товар, не скрывает его на витрине и не меняет цены или остатки.
Остаток снятого товара остаётся видимым для продажи и анализа. В автоматическом
подборе актуальных предложений отсутствующее предложение не используется;
ручное решение менеджера сохраняется отдельно с причиной и проверкой условий.
Повторное появление товара в следующем полном прайсе снимает отметку отсутствия
для этого поставщика, сохраняя историю. Если поставщиков несколько, отсутствие
у одного не означает отсутствие предложений у остальных.

В заказе показывать все допустимые варианты VE с ценой за штуку и упаковку,
потребностью, итоговым количеством и стоимостью. При одинаковой цене меньшая
упаковка может уменьшить избыточную закупку, но выбор учитывает фактическую
кратность/условия поставщика. Для режима без упаковки не считать цену подтверждённой,
если такая поставка не покрыта выбранным предложением или ручным подтверждением.
Экспорт заявки использует артикулы поставщика и явно подписанные единицы количества;
внутренний SKU остаётся в менеджерской версии. Отправка поставщику и создание
реального заказа требуют отдельного действия владельца.

Импорт должен сначала разбирать данные безопасно без выполнения макросов,
внешних ссылок и команд из ячеек. Не доверять cached formulas без проверки нужных
ценовых зависимостей. Хранить закрытый оригинал и аудит активации, применять
capability/nonce/upload limits; ошибки никогда не подменять нулевой ценой.
