# Меню категорий: локальная проверка

Проверено: **2026-09-15**, только `paint.local`. Реализация: `paint-shop-ux`.
Production, Java, серверные настройки и бизнес-данные не изменялись.
На момент проверки коммит, push и deploy не выполнялись. Локальный сайт запущен
владельцем. 2026-09-16 владелец подтвердил локальную работу и разрешил commit/push;
production deploy остаётся отдельным шагом.

## Что проверено

Настоящий локальный renderer: `WPB_Accordion_Menu_Widget::widget()` →
`wpb_wmca_accordion_pro` → `templates/taxonomy.php` → WPB category walker.
В `sidebar-1` находятся block widget и WPB category widget. Настройки последнего:
product_cat, name ASC, hide_empty=on, show_count=off, hide_out_of_stock=off.
Подмена сделана до walker, не путём удаления уже готового HTML. Другой block widget
не заменяется. [Контракт и кэш](../wp-content/plugins/paint-shop-ux/README.md).

Локальная база отличается от production: старое меню здесь содержит **1 776**
ссылок, а не 3 278 из отдельного production-аудита от 2026-09-14
(`docs/SITE_PERFORMANCE_AUDIT_2026-09-14.md`, не входит в коммит меню).
Числа из двух окружений не смешиваются. В частности, production 4,7393 с относилось
к полной sidebar через CLI, а не к обычному HTTP или таймеру одного widget.

## Начальный HTML и DOM

По семь последовательных GET для каждой страницы до изменения и после него,
без авторизации, на одной локальной базе. HTTP 200 во всех 42 ответах. Время ниже —
до получения **всего HTML** через Playwright request, не TTFB, LCP или полная
загрузка страницы. DOM посчитан браузером по полученному HTML. Первое HTTP открытие
не называется гарантированно холодным: общий кэш WordPress не сбрасывался.

| Страница | Весь HTML до → после, байт | Меню до → после, байт | Ссылки меню до → после | Медиана GET до → после, с |
|---|---:|---:|---:|---:|
| `/` | 496 457 → 175 307 | 337 555 → 18 302 | 1 776 → 30 | 1,9729 → 0,5349 |
| `/category/farbi-akrilovi/` | 476 606 → 165 619 | 337 567 → 28 412 | 1 776 → 53 | 1,8454 → 0,4303 |
| `/my-account/` | 438 919 → 99 470 | 337 555 → 273 | 1 776 → 1 | 1,7721 → 0,3634 |

Элементов внутри DOM виджета: 4 114 → 187 / 304 / 2 соответственно.
Ориентиры ≤50 КБ и ≤200 ссылок выполнены; в проверенной семиуровневой ветке
48 ссылок и также менее 50 КБ. Разметка noscript даёт дополнительную ссылку в
сыром серверном выводе, но не в DOM с включённым JS.

Все семь времён GET в порядке выполнения, секунды:

| Страница | До | После |
|---|---|---|
| Главная | 3,6861; 2,2116; 2,0858; 1,8801; 1,9085; 1,8834; 1,9729 | 1,0906; 0,5315; 0,5439; 0,5302; 0,7019; 0,5349; 0,5024 |
| Категория | 1,9214; 1,8454; 2,0409; 1,8630; 1,7898; 1,8291; 1,8049 | 0,4580; 0,4083; 0,4773; 0,4303; 0,4357; 0,4104; 0,4295 |
| Кабинет | 2,0181; 1,8952; 1,8242; 1,7399; 1,7629; 1,7409; 1,7721 | 0,3550; 0,3789; 0,3724; 0,3613; 0,3724; 0,3634; 0,3488 |

Отдельный GET ветки: 30 корней, JSON 6 298 байт, семь ответов HTTP 200:
0,5654; 0,4295; 0,4194; 0,4276; 0,4078; 0,4466; 0,4082 с.
Это уже тёплая ветка после открытия страницы; время включает bootstrap REST.
Начальный HTML не содержит всей taxonomy в скрытых узлах или JSON.

## Серверный renderer

Отдельный воспроизводимый A/B CLI-профиль на PHP **8.3.23**, без persistent object
cache. В одном процессе, по семь последовательных вызовов каждого варианта,
та же категория и настройки. Для нового widget и отдельно для branch создан
свежий namespace кэша меню; первый вызов холодный именно относительно этого кэша.
Общие term caches WordPress не очищались. Sidebar замеряется отдельным блоком
после widget-вызовов, поэтому её серия тёплая. Это **не HTTP TTFB**.

| Измерение | Первый вызов, с | Последующие 6, с | SQL первого / остальных | Размер, байт |
|---|---:|---|---:|---:|
| Старый widget | 1,454243 | 0,575811; 0,577276; 0,575133; 0,574171; 0,623838; 0,609603 | 3 / 0 | 339 345 |
| Новый widget | 0,036196 | 0,001085; 0,000896; 0,000786; 0,000754; 0,000745; 0,000737 | 14 / 0 | 28 536 |
| Старая sidebar | 0,655844 | 0,616152; 0,637423; 0,611561; 0,603992; 0,572713; 0,573537 | 0 / 0 | 340 455 |
| Новая sidebar | 0,001367 | 0,001160; 0,001098; 0,001092; 0,001076; 0,001372; 0,001242 | 0 / 0 | 29 646–29 648 |
| Новая branch | 0,024818 | 0,000047; 0,000031; 0,000029; 0,000028; 0,000028; 0,000027 | 7 / 0 | 4 245 |

Количество SQL включает чтение/запись transients и прогрев. Холодное новое меню
делает больше запросов, но строит только нужные строки; тёплый renderer использует
кэш. Cold index читает ID/parent/count всей taxonomy один раз, не полные имена,
ссылки или дерево HTML; эта стоимость не скрывается за утверждением «всегда O(1)».

## Функциональные проверки

- Полный read-only обход: **564 страницы веток**, максимум 30 строк за ответ.
  Все **1 776** term ID совпали с прежней WordPress hierarchical/hide_empty выборкой.
  Нет дубликатов; каждый результат принадлежит непосредственному родителю;
  все ссылки совпадают с get_term_link.
- Пустой лист, конец выдачи, отсутствующий родитель, неверный offset/locale;
  правильные 200/400/404. Синтетические 40 уровней и цикл завершаются корректно.
- Desktop **1440×1000**, mobile **390×1000**: текущая ветка развернута,
  aria-current и aria-expanded верны; семь уровней, переполнений текста нет.
- Быстрые открыть/закрыть/открыть дают ровно один GET. Повторное раскрытие
  загруженной ветки не запрашивает её снова. «Ще категорії» без повторов ID.
  Активная категория за первой порцией остаётся видимой и после подгрузки
  возвращается на своё место в сортировке; проверено сравнением ID с двумя API-порциями.
- Имитация 503 только в браузере: видимая ошибка, успешный ручной retry.
  Клавиатура Enter и обычные ссылки работают. При выключенном JS не остаются
  мёртвые кнопки: переход ведёт на категорию с плитками дочерних разделов.
- Кабинет и пустая корзина дают одну ссылку в каталог без menu JS; пустой checkout
  штатно перенаправляет в корзину. Ветка is_checkout покрыта кодом, но непустой
  checkout отдельно не прогонялся: заказы и содержимое корзины не менялись.
- UA проверен на реальных страницах. RU: реальный PHP renderer со switch_to_locale
  создаёт тестовую разметку вне webroot; её подставляет браузер на локальной странице,
  JS и GET веток работают с locale=ru_RU. Настройка языка сайта не менялась.
  Названия самих категорий общие, отдельной многоязычной taxonomy нет.
- Namespace кэша меняется после term invalidation. В lavka-total-sync после SQL
  recount уже есть clean_term_cache: проверено по helpers.php, синхронизация для
  теста не запускалась. Никакого Java apply.
- Существующий catalog-filters integration smoke: **12 PASS**, включая категорию,
  поставщика, SKU-поиск, ограничения выдачи и стабильную пагинацию.
- PHP lint, JS syntax check, MO compilation, git diff --check и documentation
  validation. Браузерных ошибок в проверяемых UI-сценариях нет. Существующее
  предупреждение раннего textdomain EmailKit в CLI не относится к меню.

## Скриншоты

- [Desktop](images/performance/catalog-1440.png)
- [Mobile](images/performance/catalog-390.png)
- [Семь уровней на телефоне](images/performance/deep-390.png)
- [Русская тестовая отрисовка](images/performance/ru-390.png)

## Границы и публикация

Реализован только пункт 1 аудита. Gzip, автоматический reload каталога, SLW cache
versions, ресурсы импорта и прочие пункты не изменялись. Старый accordion CSS
консервативно оставлен; JS удаляется лишь при отсутствии реально использованных
WPB shortcodes и чужих зависимостей. На мобильном сохранено положение sidebar
ниже основного контента, а не введено новое мобильное меню.

Это локальные результаты, не обещание production TTFB. На production остаётся
проверить настройки виджета, размеры его веток, активные сторонние shortcodes,
реальные роли и каталог. Особенно широкие/глубокие структуры сохраняют ссылку
на активную ветку даже сверх обычного лимита; локальная структура в лимитах.
Polylang/WPML не проверены: их сейчас нет в активном локальном окружении.

Каноническая оптовая инструкция, код «Як замовляти», UA/RU и снимок меню обновлены
локально. Production-справка **не опубликована** и не проверена под оптовой ролью.
Откат: убрать require category-menu.php из Paint Shop UX; прежние настройки WPB
сохранены и старое меню вернётся. Никаких миграций/переноса БД не требуется.

Название коммита: `perf: lazy-load catalog category branches`.

## Собственный виджет: 2026-09-17

Локальное продолжение: Paint Shop UX **1.3.0**, виджет **«Категорії Лавки»**
(`psu_category_menu`). Production, Java, товары и заказы не изменялись.
Это отдельная доработка после коммита `a813705`. После локальной проверки владелец
подтвердил работу и разрешил commit/push. Production deploy и перенос его виджетов
выполняются отдельно по инструкции владельца компонента.

### Зависимости WPB

Проверены активные sidebars, сохранённые widget options, содержимое страниц/блоков,
шаблоны и post metadata (включая Elementor), ссылки из options и собственный PHP/JS.
Поиск охватывал три shortcode плагина и его block/Elementor names из исходного кода.
Единственное активное использование локально: WPB category widget в основной sidebar.
Других используемых shortcodes/блоков не найдено. Оставшиеся ссылки в options
относятся к регистрации plugin/лицензии, сохранённым настройкам другой темы и
старому custom CSS; их не удаляли. На production этот аудит нужно повторить.

### Результат

- Настройки меню скопированы в собственный widget option: прежний заголовок,
  сортировка name ASC, hide_empty=true, show_count=false.
- Место в sidebar сохранено, соседний block widget не менялся.
- Старый экземпляр и WPB settings не удалены; резервная копия хранится до переноса.
- Перенос выполняется только явно через **Вигляд → Категорії Лавки**. Открытие
  витрины не меняет sidebar. Админка защищена capability и nonce; POST без nonce
  вернул HTTP 403. После успешного POST выполняется redirect, обновление страницы
  не повторяет операцию.
- WPB отключён **только локально**. Новое меню и REST работают без него;
  на проверенных страницах нет его CSS/JS. Для WPB, ещё не перенесённых в другом
  окружении, оставлен прежний адаптер. Автоматической деактивации WPB в коде нет.

### Проверки

- **24** локальные проверки настроек и миграции: права, устаревший план, lock,
  сохранение соседних виджетов, существующие native IDs, повторный запуск,
  конфликт отката, прерванная запись и отказ записи sidebar. Исходные options
  каждого теста восстанавливаются в finally.
- Реальный цикл: перенос через браузер → редактор виджетов → отключение WPB →
  повторное открытие редактора → включение WPB и успешный откат → повторный перенос
  и окончательное отключение WPB. Учтена штатная очистка неактивного WPB экземпляра
  редактором; остальные изменения sidebar не игнорируются при откате.
- В штатном блочном редакторе WordPress проверены название, поля и сохранённые
  значения собственного виджета до и после отключения WPB.
- Без WPB полный обход вновь дал **1 776 категорий**, **564 страницы веток**,
  максимум 30 строк за ответ и семь уровней. Без пропусков и дубликатов.
- Browser smoke: 1440×1000 и 390×1000, активная/глубокая ветка, повторные клики,
  пагинация ветки, 503 и retry, no-JS, компактный кабинет/пустая корзина, UA/RU,
  ошибки REST и отсутствие WPB assets. Ошибок JavaScript и переполнений меню нет.
- RU использует временную отрисовку PHP, язык сайта не переключался. Временная
  локальная административная сессия для теста удалена вместе с файлом cookies.
- PHP lint, JS syntax check, компиляция переводов, diff check и documentation check.
  Существующее CLI-предупреждение EmailKit не относится к этому изменению.

[Настройки собственного виджета](images/performance/category-widget-settings-2026-09-17.png)
и [меню на телефоне без WPB](images/performance/native-category-menu-mobile-2026-09-17.png).

Действия покупателя и внешний вид меню не менялись: оптовый канон и код справки
«Як замовляти» из предыдущего шага остаются актуальными, повторной правки текстов
справки не требуется. Production-проверка по-прежнему не выполнена.
Непустой checkout и отдельные языковые taxonomy WPML/Polylang не проверялись.

Инструкция переноса, блокировки и отката — в [README владельца](../wp-content/plugins/paint-shop-ux/README.md).
Предлагаемый коммит: `feat: add native Lavka category widget and WPB migration`.

## Production: переход 2026-09-17

После deploy коммита `531867a` владелец отдельно разрешил проверить сайт и отключить
старое меню. До операции на production была активна версия Paint Shop UX 1.3.0,
но sidebar ещё использовала WPB через адаптер; сам deploy не выполнил перенос.

### Предварительная проверка

- Read-only аудит активных sidebars, содержимого страниц/блоков/шаблонов, postmeta,
  options, Code Snippets и развернутого собственного PHP/JS подтвердил единственное
  действующее использование: category widget основной sidebar.
- Других вставок WPB в содержимом, builder metadata и snippets не обнаружено.
  Остались custom CSS, сохранённая схема sidebar другой темы и служебные сведения
  о плагине; их не удаляли и не меняли.
- Общий ecosystem lock и lock переноса отсутствовали. В домашнем каталоге сервера
  создана резервная копия только active_plugins, sidebar и widget options:
  `lavka-category-widget-backup-20260917-155834.json`, права 0600; чтение проверено.
  Это точечная копия настроек, не полный backup базы.

### Выполнено и проверено

- Через штатную production-страницу **Вигляд → Категорії Лавки** выполнен один
  перенос с nonce; журнал `psu_category_menu_migration_v1` имеет статус `completed`.
  Повторный план пуст, lock снят.
- Сохранены прежний заголовок, name ASC, hide_empty=true, show_count=false,
  позиция меню и соседний поисковый блок; остальные sidebar не изменились.
- После проверки native-меню отключён только `wpb-accordion-menu-or-category`.
  Файлы, исходные WPB options и резервный журнал сохранены. Другие плагины,
  Java, товары, цены, документы и заказы этой операцией не менялись.
- На production проверены главная, переход в «Фарби акрилові», активная ветка,
  раскрытие вложенной категории и подгрузка «Ще категорії». Виджет использует
  собственный ID; WPB CSS/JS на проверенных страницах отсутствуют.
- Desktop и viewport 390 px: пункты меню помещаются, раскрытие работает,
  ошибок браузера в проверяемых вкладках нет. Гостевой REST без авторизации
  возвращает следующую порцию публичных категорий с корректными ссылками.

Откат при необходимости: активировать WPB, затем воспользоваться восстановлением
на странице **Категорії Лавки** по [инструкции владельца](../wp-content/plugins/paint-shop-ux/README.md).
Не восстанавливать весь список active_plugins из backup поверх более поздних изменений.

Границы: это production smoke меню, не повтор полного локального обхода дерева,
не нагрузочный тест, не проверка непустого checkout или оптовой роли. Production-
справка «Як замовляти» в этой операции отдельно не проверялась; клиентские действия
и тексты справки не изменялись. Временные диагностические скрипты удалены с сервера,
резервная копия сохранена.

## Локальные исправления после аудита 2026-09-17

Это отдельная работа после production-переноса. Нового commit, push, deploy,
production cache flush или записи в бизнес-данные не было. Исходный HEAD:
`531867a`. Чужие изменения документов в worktree сохранены.

### 1. Контекст ссылок меню

Повторный аудит обнаружил зависимость общего кэша от первой открытой страницы:
быстрый заказ мог сохранить свои URL и затем отдать их обычному каталогу/REST.
Предыдущий smoke проверял отдельные переходы, но не обе очередности прогрева.

- В общем кэше теперь только канонические category URL и slug. Контекстные ссылки
  строятся после чтения кэша; JS передаёт публичный ID quick-order страницы в REST.
- Единственный владелец rewrite — `PCQO_Category_Links` в quick-order MU.
  Дубль в child theme удалён; посторонние `term_link` filters сохранены.
- Меню 1.3.1 меняет только свой cache namespace. Записи версии 1.3.0 больше не
  используются; очистка общего кэша не требуется.
- До исправления regression suite воспроизвёл **28 ошибок из 84**. После него
  расширенный suite прошёл **92/92**: обе очередности, guest/customer/shop_manager
  и настроенные оптовые роли, UK/RU, initial/REST/следующая порция/дети, глубокая
  активная ветка, no-JS HTML и чужой term-link filter. Роли имитируются в памяти;
  пользователи и права не записываются. Это не браузерная авторизация под ролями.

### 2. Размер страницы без автоматического reload

- Из `psu-force-per-page.php` удалены измерение колонок, cookies и принудительный
  reload. Один resolver обслуживает `pp`, legacy `per_page` и default24.
- Оба поля основного query согласованы. Переключатель 12/24/48 сохраняет фильтры
  и возвращает на первую страницу, форма фильтров сохраняет валидный выбор.
- **18/18** локальных проверок resolver/query; исходный resolver проваливал
  9 из 11 проверок параметров. **39/39** read-only HTTP-проверок: размеры
  12/24/48/20, реальные страницы 1/2, непересекающиеся товары, category/stock/sort,
  старые cookies, отсутствие удалённого скрипта в HTML.
- Browser smoke: обычные URL initial/подгружаемой ветки, desktop и 390×844,
  24→12 товаров, мобильная страница 2, затем 48 товаров с возвратом на страницу 1,
  сохранение выбора после resize. В журнале вкладки нет JavaScript errors.
  На 390 px body не шире viewport, переполнений ссылок/кнопок меню нет.
  Инструмент браузера периодически возвращал CDP timeout при навигации;
  результат перехода проверялся отдельным чтением страницы.

### Завершение локальной приёмки 2026-09-17

Повтор выполнен на текущем worktree без переписывания application patch. Добавлены
воспроизводимые browser fixtures и парный benchmark. Первая попытка нового теста
прочитала DOM до окончания загрузки после перехода в quick-order категорию;
исправлено ожидание в тесте, дефекта application-кода не обнаружено.

- Свежий `category-menu.spec.cjs` прошёл на 1440 и 390 px: 503/retry, клавиатура,
  дедупликация запросов и строк, collapse/reopen, ветка до седьмого уровня,
  no-JS, компактные пустые поверхности и отсутствие WPB assets. RU-фикстура заново
  отрисована текущим PHP, язык сайта не переключался; это не проверка отдельных
  WPML/Polylang taxonomy. Ошибок JavaScript и переполнений меню не обнаружено.
- `catalog-browser-acceptance.cjs` записал Network с момента до первой навигации.
  На **каждой** ширине: первое открытие **1** document GET; resize в обе стороны
  **0**; каждое переключение 12/24/48 **1**; страница 2 **1**. Все десять document
  запросов двух сценариев получили HTTP 200 без redirect. Подтверждены фактическое
  число товаров, возврат на страницу 1 и сохранение category/stock/sort.
- В новой браузерной сессии существующего локального оптового пользователя реально
  отобразилась таблица quick-order. Пройдены обе очередности прогрева, начальные,
  подгружаемые, следующие и глубокие ссылки; гостевой каталог/REST остаются
  каноническими. Гость и retail не получили доступ к quick-order, ссылки меню у
  них обычные. Отдельная сессия shop_manager недоступна, её browser case пропущен
  явно; прежние имитационные PHP-проверки роли не выдаются за browser acceptance.
- Повторный полный PHP-обход: **1 776 категорий**, **564 страницы веток**, максимум
  **30 строк**, глубина **7**; без пропусков/дубликатов. Пройдены проверки
  namespace invalidation и синтетических глубины/циклов.
- Под оптовой сессией read-only GET «Як замовляти» вернул 200, anchor `katalog`
  и актуальное пояснение 12/24/48. Это проверка локального HTML, не публикация и
  не отдельная визуальная приёмка справки на production.
- Временные токены отозваны, файл с cookies удалён. Роли/профили не изменялись.
  Во время тестов менялся только локальный namespace меню; общий кэш не очищался.
  Последующее invalidation другого теста cleanup не перезаписывал.

### Сопоставимый замер renderer

`category-menu-native-benchmark-local.php` сравнил `531867a` и текущий renderer
в **одном** WP-CLI процессе PHP 8.3.23: семь чередующихся пар по 100 тёплых
рендеров для каждой поверхности. Baseline-класс переименован, его hooks не
регистрируются. WPB не активировался, общий кэш не очищался.

| Поверхность | До, медиана мс/рендер | После, медиана мс/рендер | Разница | SQL, тёплый batch |
|---|---:|---:|---:|---:|
| Главная | 0.5390 | 0.5468 | +1.44% | 0 / 0 |
| Категория | 0.6710 | 0.6713 | +0.04% | 0 / 0 |

Ориентир отсутствия регрессии более 10% для тёплого renderer в этой серии пройден.
Это не ускорение сайта и не отдельный холодный PHP startup. Первые namespace
builds зависели от общих WordPress core caches и порядка: главная до/после
30.05/15.09 мс, категория 14.72/27.46 мс. Их нельзя выдавать за сопоставимую
холодную серию. Ранее наблюдавшийся разброс отдельных микрозамеров не используется
для вывода о регрессии; парная методика уменьшает влияние окружения.

### Отдельная HTTP-серия

После завершения browser/PHP-тестов `catalog-http-benchmark.cjs acceptance-final`
выполнил семь последовательных гостевых GET на поверхность без других наших
HTTP-тестов и без изменения server config. Запросы явно допускают gzip/deflate/br.
Все 28 ответов HTTP 200, **Content-Encoding: gzip**, сервер локального runtime
nginx/1.26.1. Размеры ниже относятся к распакованному телу, не к сетевому трафику.

| Поверхность | TTFB медиана, с | TTFB min–max, с | Всё тело, медиана с | Тело, байт |
|---|---:|---:|---:|---:|
| Главная | 0.520 | 0.507–2.121 | 0.523 | 172 023 |
| Категория | 0.380 | 0.364–0.426 | 0.383 | 162 079 |
| Кабинет, гость | 0.342 | 0.335–0.608 | 0.351 | 99 268 |
| REST | 0.407 | 0.404–0.513 | 0.408 | 7 143 |

Меню HTTP: главная **17 700 байт / 30 ссылок**, категория **27 619 / 53**;
до контекстной правки было 17 674 / 27 593 байта. REST прежде занимал 6 298 байт:
добавлен slug, лимит 30 сохранён. Ограничение начального меню 50 КБ соблюдено.

Предыдущие локальные серии имели медианы 2–6 с, единичный GET категории 186.8 с.
Причина колебаний не установлена, прежние заголовки сжатия не сохранялись. Новая
серия не превращает их в достоверное A/B-сравнение. **Процент ускорения полного
HTTP, production TTFB, LCP/INP и мобильная сеть не заявляются.** Устойчиво
подтверждено устранение лишнего document request и корректность пользовательского
сценария; сопоставимость времени renderer обеспечена отдельным парным тестом.

Артефакты: `/tmp/psu-catalog-browser-acceptance/network-and-results.json`,
`/tmp/psu-browser-rerun-20260917/`, `/tmp/psu-native-paired-benchmark.json`,
`/tmp/psu-http-acceptance-final.json`. Network JSON содержит только URL/метод/статус
и относительное время, без cookies и приватных тел. Предыдущие context/query/HTTP
результаты (92 + 18 + 39 проверок) сохранены отдельно. Scripts находятся в
`paint-shop-ux/tests/`; подготовка и обязательная очистка сессий описаны в README.

PHP lint, JS syntax, компиляция UK/RU, skill validator, `git diff --check` и
documentation-impact check пройдены. Существующее предупреждение EmailKit и
отсутствующие metadata PO не относятся к этим правкам. Канон оптовой инструкции и
код справки обновлены локально, production-публикации этой правки не было.

### Границы и следующий шаг

Локальные Network/browser и парный renderer пункты закрыты с указанным исключением
shop_manager. На production версия 1.3.1 ещё не проверена и не опубликована.
В рамках завершения приёмки не выполнялись production reads/writes, commit, push,
deploy, синхронизация или изменения Java, товаров, заказов, оплат и корзин.
Перед rollout повторить ключевой smoke и отдельно разрешить deployment.

Следующая узкая локальная задача для обсуждения с владельцем скорости: стабильные
версии frontend CSS/JS Stock Locations вместо `time()`, через собственный слой
совместимости без правки vendor. Код-владелец:
`stock-locations-for-woocommerce.php::enqueue_frontend()`. Приёмка: одинаковый URL
неизменённого ресурса в последовательных ответах, изменение версии после обновления
файла/plugin, browser cache hit на повторном визите и отсутствие регрессии склада,
карточки и каталога. Сначала inventory handles/dependencies, никаких слепых dequeue.

Сжатие production остаётся отдельной инфраструктурной проверкой из аудита скорости:
локальный gzip уже включён и не доказывает состояние production Apache. Повторная
проверка/изменение production в текущую локальную приёмку не входят.

Логические названия будущих коммитов: `fix: isolate category menu URL context`;
`perf: remove catalogue reload and stabilize pagination`.
